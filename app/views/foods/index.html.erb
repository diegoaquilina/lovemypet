
<div class="container">
  <h1>Alimentaçao do(s) seu(s) pet(s)</h1>

  <% if @foods.empty? %>
    "Voce nao possui nenhuma ração registrada."
  <% else %>
    <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th>Pet</th>
                <th>Tipo de raçao</th>
                <th>Marca</th>
                <th>Qtd do pacote</th>
                <th>Qtd diária</th>
                <th>Data de inicio</th>
                <th colspan="3"></th>
            </tr>
            </thead>

            <tbody>
                <% @foods.each do |food| %>
                  <tr>
                    <td><%= food.pet.name %></td>
                    <td><%= food.category %></td>
                    <td><%= food.brand %></td>
                    <td><%= food.bag_qty_kg %></td>
                    <td><%= food.daily_qty_kg %></td>
                    <td><%= food.start_date %></td>
                    <td><%= link_to 'Editar', edit_food_path(food) %></td>
                    <td><%= link_to 'Deletar', food_path(food), data: {turbo_method: :delete, turbo_confirm: "Você tem certeza?"} %></td>
                  </tr>
                <% end %>
            </tbody>
        </table>
    </div>
  <% end %>
</div>
