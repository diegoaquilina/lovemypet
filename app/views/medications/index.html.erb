<div class="container">
    <h1>Medicamento(s) do(s) seu(s) pet(s)</h1>

    <% if @medications.empty? %>
        "Seu pet nao possui nenhum medicamento."
    <% else %>
      <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th>Pet</th>
                <th>Medicamento</th>
                <th>Dose</th>
                <th>Unidade da dose</th>
                <th>Instruções</th>
                <th style="text-align:center">Frequencia/hora</th>
                <th style="text-align:center">Periodo a serem tomado</th>
                <th colspan="3"></th>
            </tr>
            </thead>

            <tbody>
                <% @medications.each do |medication| %>
                    <tr>
                    <td><%= medication.pet.name %></td>
                    <td><%= medication.name %></td>
                    <td style="text-align:center"><%= medication.dose %></td>
                    <td><%= medication.dose_unit %></td>
                    <td><%= medication.instructions %></td>
                    <td style="text-align:center"v><%= medication.frequency %>/hrs</td>
                    <td style="text-align:center"><%= medication.days_qty %></td>
                    <td><%= link_to 'Editar', edit_medication_path(medication) %></td>
                    <td><%= link_to 'Deletar', medication_path(medication), data: {turbo_method: :delete, turbo_confirm: "Você tem certeza?"} %></td>
                    </tr>
                <% end %>
            </tbody>
        </table>
      </div>
    <% end %>
</div>
