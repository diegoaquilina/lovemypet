<div class="d-flex justify-content-center align-items-center">
  <h1>🛁Editar Banho & Tosa</h1>
</div>

<div class="container bg-light rounded-5 p-2">
  <div class="bg-light rounded-5 px-4 py-2">
    <%= simple_form_for @bath do |f| %>
     <div class="col-12">
     <%= f.input :pet, collection: @pet.map { |pet| [pet.name.capitalize, pet.id]}, include_blank: false %>
     </div>
    <div class="row">
     <div class="col-md-4">
      <%= f.input :category, label: "Serviço", collection: Bath::SERVICE.map { |e| e.capitalize}, include_blank: false %>
     </div>
      <div class="col-md-8">
       <%= f.input :address, label: "Petshop" %>
      </div>
    </div>
    <div class="col-12">
     <%= f.input :date, order:[:day, :month, :year],start_year: Date.today.year,
        end_year: Date.today.year + 1, label: "Data" %>
    </div>
    <div class="col-12">
      <%= f.check_box :notification %> Ativar lembrete desse serviço
    </div>
    <div class="col-12">
     <div class="d-flex justify-content-center align-items-center">
      <%= f.submit "Editar", class:"btn btn-gradient" %>
     </div>
    </div>
  </div>
  <% end %>
</div>
